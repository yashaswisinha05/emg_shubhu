% Requires \usepackage{svg} in the document preamble.
% The SVG is vector-native and should be placed in the paper's figures folder.
\begin{figure*}[t]
    \centering
    \includesvg[width=\textwidth]{figures/final_model_pipeline}
    \caption{Proposed causal shared-encoder model. Frozen modality features and
    gripper state condition a bounded sEMG correction to the inertial motion
    representation. The decoder estimates current position, screen location,
    and short- and long-horizon motion. Future targets are used only during
    training.}
    \label{fig:final-neuromuscular-model}
\end{figure*}
