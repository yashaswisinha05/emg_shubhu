seed: 42
experiment_name: "grid_point"

paths:
  data_root: "../MERGED DATA"
  artifacts_dir: "artifacts"
  manifest: "artifacts/manifest.csv"
  cache_dir: "artifacts/cache"
  split_file: "artifacts/trajectory_cv/a1/fold-0/split.json"
  scaler: "artifacts/grid_point/a1/fold-0/scaler.npz"
  output_dir: "runs/grid_point"

data:
  sample_rate_hz: 148.14814813792788
  median_kernel: 3
  emg_log1p: true
  target: "click"
  trajectory_end: "touch_time"
  temporal_anchor: "touch_time"
  imu_calibration_window_s: 0.3
  min_duration_s: 0.25
  max_duration_s: 10.0
  outlier_policy: "exclude"
  subject_aliases: {}

cross_validation:
  folds: 5
  stratification_grid: [8, 5]

model:
  grid_size: [8, 5]
  d_model: 128
  num_layers: 3
  num_heads: 4
  ffn_dim: 384
  dropout: 0.15
  patch_length: 16
  patch_stride: 8
  tcn_kernel_sizes: [3, 7, 15]
  imu_endpoint_s: 0.5
  emg_long_s: 0.5
  emg_short_s: 0.3

loss:
  gaussian_sigma_cells: 0.5
  heatmap_weight: 1.0
  offset_weight: 1.0
  pixel_weight: 0.5
  radial_weight: 0.25
  offset_huber_beta: 0.1
  pixel_huber_beta: 0.25
  pixel_normalizer_px: 80.0
  charbonnier_epsilon_px: 1.0

training:
  batch_size: 32
  bucket_size_multiplier: 10
  num_workers: 0
  epochs: 30
  learning_rate: 0.0003
  weight_decay: 0.0001
  gradient_clip_norm: 1.0
  patience: 8
  scheduler_patience: 3
  scheduler_factor: 0.5
  minimum_learning_rate: 0.000001
  amp: true

grid_sweep:
  epochs:
    grid_imu: 30
    grid_emg: 30
    grid_fusion: 15
  patience:
    grid_imu: 8
    grid_emg: 8
    grid_fusion: 6
